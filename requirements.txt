streamlit>=1.35.0
gspread>=6.0.0
google-auth>=2.29.0
pytz>=2024.1

# Optional, only needed if you want the live countdown timer to
# auto-refresh the screen every second. The app works fine without
# it — the timer just won't tick until the next natural interaction.
streamlit-autorefresh>=1.0.1

# Optional, only needed for the SPEAKING question type's microphone
# recorder. Without it, SPEAKING questions fall back to a text box.
streamlit-mic-recorder>=0.0.8
